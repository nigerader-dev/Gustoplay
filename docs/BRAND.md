# Бренд: название, домен, позиционирование

## Выбор

**GustoPlay** — «играй со вкусом». Русская подпись: **GustoPlay — подбор игр по вкусу**.

Почему именно так:

- **Про вкус, а не про поиск.** «Gusto» — это вкус и удовольствие: сайт не «ищет игру»,
  а подбирает её под ваш вкус. Это ровно то, что делает квиз, — название описывает продукт.
- **Работает на двух языках.** Латиница читается одинаково у русскоязычной и англоязычной
  аудитории, пишется по памяти без ошибок (важно для прямых заходов, а это самые дешёвые визиты).
- **Быстро произносится** (4 слога), годится и в домен, и в ник в соцсетях, и в голосовое упоминание.
- **Не привязано к жанру.** Не «игроискатель» и не «каталог» — звучит как бренд, а не как функция,
  поэтому масштабируется на подборки, обзоры, календарь релизов и приложение.
- **Домен повторяет бренд**: gustoplay.ru — услышал название, записал адрес, ничего переводить не надо.
- **Есть естественная иконка**: 🎮 уже используется в шапке и фавиконе; позже можно нарисовать
  «джойстик с языком вкуса» в SVG.

## Домены (по приоритету)

| Зона | Почему | Ориентир цены |
|---|---|---|
| `gustoplay.ru` | основной: повторяет бренд, дешёвый, «родной» для русскоязычной аудитории и рекламы | ~200–400 ₽/год |
| `gustoplay.com` | международное зеркало, если пойдём на англоязычную аудиторию | ~$10/год |
| `gustoplay.рф` | юридическое зеркало для РФ, письма и маркировка рекламы | ~200 ₽/год |

Важно: зону `.ru` нельзя купить у Cloudflare Registrar — только у аккредитованного регистратора
(reg.ru, nic.ru, timeweb и т.п.). Схема: покупаете домен у регистратора → деплой сам добавляет зону
в Cloudflare → у регистратора меняете NS на выданные Cloudflare (скрипт печатает их при деплое) →
домен и маршрут `/api/*` подключаются автоматически. Перед покупкой проверьте имя в
Instagram/TikTok/Telegram — лучше занять его сразу, даже пустым.

## Что поменять, чтобы переименовать сайт

Название живёт в одном месте — `js/config.js`:

```js
export const SITE = {
  name: 'GustoPlay',
  domain: 'gustoplay.ru',
  url: 'https://gustoplay.ru',
  email: 'hello@gustoplay.ru',
  tagline: { ru: 'Подбор игры по вкусу за 60 секунд', en: 'Find your next game in 60 seconds' },
  ...
};
```

После правки достаточно `npm run build` — обновятся title, OG-теги, canonical, sitemap, подвал и шапка.
Заголовки, OG-теги и подвал берут имя из `SITE.name`, поэтому любое переименование в будущем —
это правка нескольких строк в `js/config.js` и идентификаторов в `tools/deploy-cloudflare.mjs`
(имена базы D1, воркера и проекта Pages).

История названий: **GameFinder → PlayNext → GustoPlay** (сентябрь 2026).

## Голос бренда

- Обращение на «вы», без панибратства; тон — как у знающего друга: «вот это подойдёт, потому что ты любишь…».
- Никаких «гениально», «шок», «вы не поверили». Мы объясняем, а не продаём.
- В рекомендациях всегда есть причина: «короткая», «отлично играется компанией», «есть мод-поддержка».
- Ошибки и пустые состояния — человеческим языком: не «No results found», а «Под эти фильтры игр нет. Ослабим бюджет?».

## Что куда писать (каналы)

| Канал | Что публиковать |
|---|---|
| Telegram-канал **GUSTOplay.ru** ([приглашение](https://t.me/+K915edYs7C1iNTEy)) | «Что поиграть в выходные», подборки на 4 игрока, свежие релизы |
| Сайт `/party` | Ссылки для компании — их удобно кидать в чат друзьям (главный виральный сценарий) |
| YouTube Shorts / Reels | «3 игры под ваш вечер» длиной 30 секунд с указанием сайта |
| Reddit / Discord | Ответы по делу в r/ShouldIbuythisgame, r/coopgaming с ссылкой на конкретную подборку, не на главную |

Канал пока приватный (ссылка-приглашение). Когда появится публичный хэндл (например, `@gustoplay`),
поменяйте `SITE.telegram` и `ADS.house.url` в `js/config.js` на постоянный адрес `https://t.me/gustoplay`.

## Визуальный стиль (уже реализован)

- Светлая минималистичная тема по умолчанию, тёмная — переключателем; акцент `#5b5bd6` (индиго),
  в тёмной теме акцент осветляется до `#8b8bff` для контраста.
- Иконки — эмодзи: не нужно тянуть шрифты и иконочные наборы, страница весит меньше, работает всегда.
- Обложки игр генерируются алгоритмом из названия (детерминированно, без авторских прав).
  Когда появятся права на официальные арты — включите `FEATURES.realCovers` и положите файлы в `covers/<slug>.jpg`.
