# Контент-план: полная вычитка сайта и сдача «под ключ»

> **Состояние на 26 сентября (вечер).** Всё, что ниже, закоммичено и запушено в
> `arena/01a0da20-gustoplay` (последний коммит — `45f3410`); на GitHub открыт PR #5,
> мержит его пользователь. Осталось одно честно нерешаемое ограничение — см. последний раздел.
> Напоминание по окружению: в песочнице нет интернета к Steam и магазинам (ответ 000), поэтому
> ссылки проверяет CI; браузер для визуального QA поднимается так:
> `node tools/chromium-offline.mjs` → путь к Chromium и `LD_LIBRARY_PATH` он печатает сам.

Составлен 25 сентября 2026 по итогам сообщения пользователя и разведки кода.
Статус пункта меняется по мере работы: ⬜ не начато · 🔄 в работе · ✅ сделано · ⚠️ ограничение (объяснено).

| № | Что проверить / сделать | Статус | Доказательство |
|---|---|---|---|
| 1 | **Мерцание кнопок отметок при наведении** («понравилось», «не понравилось» скачут и мигают, «играл» и «хочу сыграть» — нет) | ✅ | Причина найдена в браузере: `.mark span` менял `display: none → inline` на hover, кнопка росла с 44 до 167px, соседние переносились на другую строку (`left 537→343`), курсор терял кнопку → цикл `mouseover/mouseout` (мерцание). Исправлено: подпись всегда вне потока (текст остаётся для скринридера), при наведении меняется только цвет, подсказка — нативным `title`; убран и 1px-сдвиг по наведению (у края он давал тот же цикл). Регресс-проверки: `test:visual` — под курсором размер/позиция не меняются, `mouseenter` ровно один, `elementFromPoint` возвращает ту же кнопку; `test:css` — у `.mark:hover` нет правил раскладки |
| 2 | **Шторка меню на телефоне**: не закрывает ли пол-экрана, можно ли убрать кнопкой | ✅ | Было 88vw: на 360px видно 43px страницы (12%). Стало 80vw: панель 288px, видно 72px. Крестик 44×44, закрывает и возвращает фокус на бургер; закрывают также Esc, затемнение и переход по ссылке. Проверки в `test:visual` (доля экрана, видимая полоса, крестик, фокус, Esc, затемнение, блокировка скролла, отсутствие горизонтальной прокрутки) |
| 3 | **Убрать сторонний магазин Instant Gaming** | ✅ | Удалён из данных (`js/catalog/index.js`), интерфейса (`storeLinks`), i18n (`game.instant`), конфига (`SITE.affiliates.instantGaming`) и документации (`LAUNCH.md`, `MONETIZATION.md`). `grep -ri "instant gaming"` по сайту — пусто |
| 4 | **Конкретная ссылка на страницу игры в Steam у каждой игры** | ✅ | Найден и исправлен баг: кнопка всегда вела на **поиск по названию** — appid читался из сырой записи, а он приходит из `steam-covers.js` и подмешивается ниже, поэтому условие не выполнялось никогда. Сейчас ссылку на конкретную страницу имеют **435 игр из 436**: 401 — страница игры в Steam (`store.steampowered.com/app/<id>/`), 34 — официальные магазины издателей (Nintendo eShop, PlayStation Store, Battle.net, Xbox, App Store, HoYoverse, Supercell, сайты издателей). Проверено расчётом по данным (`storeLinks(g).length > 0`); выдуманных адресов нет — каждый кандидат проверяется в CI (`links.yml`: HTTP-статус + название в `og:title`/`title`/`twitter:title`/JSON-LD) и только потом попадает в данные через `npm run stores:pull --apply`. Без ссылки осталась одна игра — Football Manager 2024: снята с продажи в Steam и Epic 05.11.2025, официальная страница отдаёт роботам 403, заголовок раздела `/games` — «Football Manager 27». Пока страницы нет, кнопки нет: это лучше поисковой заглушки. Попутно починены две ошибки самой проверки: аннотация GitHub режется на 4096 символах (теперь список идёт частями `PART_i_OF_n`, `tools/pull-store-links.mjs` их склеивает), а `--apply` заменял файл целиком и стирал ранее подтверждённые адреса (теперь данные сливаются — так вернулся PlayStation-адрес Bloodborne) |
| 5 | **Никаких поисковых ссылок-заглушек** в блоке покупки | ✅ | `grep '/search/'` по каталогу — 0 совпадений; smoke-тест проверяет, что ссылка в Steam ведёт на `/app/<id>/`, а поисковых ссылок нет. Нет конкретной страницы — кнопки нет вовсе |
| 6 | **Орфография и пунктуация RU+EN** на всех страницах и в контенте 436 игр | ✅ | `npm run check:text` (входит в `test:all`): словари RU и EN/GB офлайн + правила типографики + единый стиль английского; 5100+ текстов. Найдено и исправлено: «ложня» → «ложь», «превщают» → «превращают», «Рогалевания» → «Рогалик-метроидвания», «Рогалайк» → «Рогалик», «фантастка и фэнтезистка» → «автор научной фантастики и автор фэнтези», `tower-defense` → `tower defence`, `civilization` → `civilisation`. В интерфейсе: «в вишлисте» → «хочу сыграть», «Пересчёт мгновенный и обратимы» → согласовано. Весь русский интерфейс прочитан целиком. Дополнительно: проверка ключей — каждый `t('…')`/`tp('…')` из кода должен существовать в обоих языках, множественные формы ru (.one/.few/.many) сверяются с en (.one/.other) — так нашлись `game.store*` без подписи. В последнем заходе добавлены правила про смешение языков: кириллица в английском тексте и русский текст без кириллицы (по пути поля видно язык). Прогон: 5171 текст, 0 ошибок; смешанных полей в каталоге 0, пустых `about` 0, самая короткая английская аннотация — 0.78 от русской (Valorant), короче 90 символов нет. Исправлены повторы и неточности: «Покерный рогалик, в котором правила покера» → «Рогалик, в котором правила покера», «На неделю-другую вечеров» → «Хватит на неделю-другую», «{n} кооп за одним экраном» → «{n} игр с коопом за одним экраном», а также текст «обложки генерируются нашим алгоритмом» — это было неправдой, обложки официальные (Steam, Nintendo eShop, Epic, магазины издателей) |
| 7 | **Скачки текста** (сдвиги при загрузке и взаимодействии), CLS | ✅ | Замеры `layout-shift` в браузере: загрузка каталога на 360 и 1440 — CLS 0.000; карточки после подгрузки обложек не двигаются (dx=dy=0); открытие меню и наведение на отметки — CLS 0. Проверки в `test:visual` |
| 8 | **Дистанции и выравнивание текста**: не наезжает ли на соседей, попадает ли в свои блоки, обрезка длинных строк | ✅ | На 360px описание карточки обрезалось («Пирамидоголовый, туман и вина:» — 145px в 133px) — исправлено переносом длинных слов. В последнем заходе нашлись ещё два места: строка отметки в «Моём вкусе» была шире панели (364px в 294px — колонка грида растягивалась под неразрывное название, подпись статуса уезжала за край экрана; теперь колонка `minmax(0, 1fr)`, а на телефоне строка — сетка «обложка \| название и статус \| крестик»), и подписи шкал интересов обрезались многоточием (колонка была жёстко 150px — «Сплитскрин/локальный кооп» и ещё 7 тегов из 10 не читались целиком ни на 360px, ни на 1024px; теперь колонка по содержимому, на телефоне подпись над шкалой — обрезанных 0 из 10 на всех ширинах). Регрессы: `test:visual` обходит 5 маршрутов на вылет за экран, `test:css` проверяет обе новые правила |
| 9 | **Общий обход сайта на баги**: все маршруты, все ширины, консоль, ссылки, состояния (пустое, ошибка, 404) | ✅ | `test:visual`: 17 маршрутов × 5 ширин (360/768/1024/1440/1920) — **85 проверок, ❌ 0** + интерактивный блок в браузере с включённым hover (наведение на отметки, шторка меню на 360/768/1141, сдвиги макета, обрезка текста, вылет за экран, липкая панель квиза); скриншоты в `docs/qa-shots` (в git не коммитятся). `test:functional` — 120/120 |
| 11 | **Найдено при проверке: шапка десктопа** — шесть пунктов меню не влезали в строку (901–1920px), последний уезжал на вторую строку | ✅ | Замерено в браузере: было headerH=111px, navH=87px, две строки, справа пустое место. Стало: рабочая ширина 1280px, подпись логотипа не переносится и скрывается ниже 1500px, пункты меню плотнее и без переноса, шторка включается с 1140px. Итог — одна строка на 12 ширинах (1920…1141), headerH=68px. Регресс: `test:css` §5б (4 проверки: `.nav` без переноса, подпись логотипа скрывается, компактные пункты, бургер до 1140px) |
| 12 | **Найдено в последнем заходе: строки «Моего вкуса» на телефоне вылезали за экран** | ✅ | На 360px строка отметки была 364px в панели 294px: колонка грида растягивалась под неразрывное название игры, правый край с подписью статуса обрезался краем экрана (сама страница вбок не ехала — вылет скрывал `overflow-x: clip`, поэтому прошлые проверки его не видели). Теперь колонка `minmax(0, 1fr)`, строка сжимается, на узких экранах — сетка «обложка \| название и статус \| крестик», длинные названия переносятся. Замер после правки: строка 294px в панели 294px, подпись статуса целиком в экране |
| 13 | **Найдено в последнем заходе: подписи шкал интересов были обрезаны многоточием** | ✅ | «Сплитскрин/локальный кооп» и ещё 7 подписей из 10 не читались целиком ни на 360px, ни на 1024px — колонка подписи была жёстко 150px. Стала по содержимому (`max-content`), на телефоне подпись встаёт над шкалой. Замер: обрезанных подписей 0 из 10 на 360/480/640/768/1024. Регресс в `test:css` |
| 14 | **Найдено в последнем заходе: панель «Дальше» в квизе просвечивала** | ✅ | Фон был полупрозрачным (90%) без размытия: под липкой панелью проезжала карточка вопроса, текст читался сквозь кнопки. Теперь фон с размытием, как у шапки, и сплошной фолбэк без `backdrop-filter`; проверка в `test:visual` (панель на 360/480 липнет к низу, кнопка не обрезана, фон не просвечивает без размытия) и правило в `test:css` |
| 16 | **Найдено в последнем заходе: очень узкие экраны (280–320px, сгибаемые телефоны)** | ✅ | На 280px шапка была 310px — кнопка меню уезжала за край экрана, открыть меню было нельзя; на 320px три кнопки панели квиза не влезали в строку, «Дальше» уходила за экран на 21px. Исправлено: на ≤320px скрывается название бренда (иконка остаётся), на ≤360px кнопки квиза компактнее, перенос кнопок — страховка на совсем узкие экраны. Проверка: 11 маршрутов × 280/320 — вылета за экран и обрезанного текста нет; `test:visual` теперь обходит 5 маршрутов на 280/320/360/768 |
| 17 | **Найдено в последнем заходе: слова и цены рвались посередине в карточках** | ✅ | На 360px студия «ConcernedApe» рвалась на «ConcernedA» и «pe», цена «2500 ₽ и выше» и длительность «50–200» ломались внутри числа — 16 разорванных слов на первом экране. Цена разделена на две группы, «год · студия» переносится целыми словами, подвал карточки не рвёт числа; в описаниях включены переносы по слогам (`hyphens: auto`) со страховкой `break-word`. Проверка `test:visual` §З: на 360/480 в карточках не рвутся числа, цены и подписи |
| 18 | **Доступность: порядок заголовков перескакивал на всех страницах** | ✅ | Нашёл аудит Lighthouse (heading-order): h1 «Каталог игр» → h3 карточки, h2 → h4 в подвале, h1 → h4 в «Условиях». Уровень заголовка теперь выбирается по структуре страницы, а размер — по роли (классы `.h-lg`/`.h-base` сохраняют прежний вид). Проверено в браузере: 14 маршрутов идут последовательно; регресс в `test:a11y` (перебор уровней на всех маршрутах) |
| 19 | **Доступность: контраст мелкого текста и имя ссылки-обложки** | ✅ | Контраст: `--text-muted` на «утопленном» фоне давал 4.24 при пороге 4.5 (блок рекламы) — стало 4.53, пары `text-soft/text-muted` на `bg-sunken` добавлены в проверку. Имя: видимые оценка («96») и «1–4» лежали внутри ссылки-обложки, а `aria-label` содержал только название (WCAG 2.5.3) — обложка, оценка и плашка игроков теперь соседи в обёртке, плашки не перехватывают клик |
| 20 | **Производительность сборки: CSS и JS уезжали несжатыми** | ✅ | Аудит Lighthouse на собранном сайте: 21 КиБ лишнего в CSS и 70 КиБ в JS. Теперь `npm run build` минифицирует CSS (пакет `css`, со сверкой числа объявлений — иначе сжатие откатывается) и JS (`esbuild`, `charset: utf8` — без него кириллица экранировалась и файлы росли вдвое). Замер на собранном сайте: performance **67 → 94**, FCP 4.1 → 2.1 с, LCP 6.7 → 2.8 с, accessibility **100**, SEO **100**, CLS 0. `verify:build` проверяет, что сборка действительно сжата; `test:visual` умеет `--dir=dist` |
| 15 | **Итоговый прогон и CI** | ✅ | `npm run test:all` — зелёный целиком: check (436 записей, проблем 0), check:text (5172 текста, 0 ошибок; ключи 297 ru = 297 en), audit (436 корректны), smoke ✅, functional 120/120, a11y 18/18, css 59/59, visual 85 проверок «маршрут × ширина» + 95 интерактивных (❌ 0), api 55/55, e2e 20/20, reload 8/8. Прод-сборка: 624 страницы, минифицирована, `verify:build` 29/29. Аудит Lighthouse на собранном сайте: performance 94, accessibility 100, best-practices 96, SEO 100, CLS 0 |

## Найдено и исправлено в последнем заходе (ссылки доведены, мелочи вёрстки закрыты)

**Строки «Моего вкуса» на телефоне.** На 360px строка отметки была 364px в панели 294px:
колонка грида растягивалась под неразрывное название игры, правый край с подписью статуса
обрезался краем экрана. Страница при этом вбок не ехала — вылет скрывал `overflow-x: clip`,
поэтому прошлые проверки (они смотрели только каталог) его не видели. Исправлено, добавлена
проверка «ничего не вылезает за экран» на 360 и 768 для пяти маршрутов.

**Подписи шкал интересов.** Колонка подписи была жёстко 150px, поэтому 8 из 10 тегов
(«Сплитскрин/локальный кооп», «Не требует мощного ПК», «Высокая реиграбельность»…) обрезались
многоточием на всех ширинах. Теперь колонка по содержимому, на телефоне подпись над шкалой —
обрезанных подписей 0 из 10 на 360/480/640/768/1024.

**Панель «Дальше» в квизе.** Была полупрозрачной без размытия: под липкой панелью проезжала
карточка вопроса и текст читался сквозь кнопки. Теперь фон с размытием, как у шапки, плюс
сплошной фолбэк там, где `backdrop-filter` не поддерживается.


**Выдуманная игра в каталоге.** Проверка ссылок показала пару «Va-11 Hall-A: Second Round →
N1RV Ann-A: Cyberpunk Bartender API» (совпадение названий 0.00). Разобрался: «Second Round» —
это саундтрек Garoad 2016 года, а не игра; продолжение VA-11 HALL-A — N1RV Ann-A, и оно
бессрочно отложено (страница в Steam без даты выхода). В каталоге была запись «Va-11 Hall-A:
Second Round, 2026, Sukeban Games» с описанием «продолжение барменской классики» — игры
с таким названием не существует. Запись удалена из каталога, контента, обложек и overrides;
игр в каталоге стало 436. Тесты, где число игр было прошито константой, переведены на
`GAMES.length` — иначе они падали бы при любом изменении каталога.

**Проверка ссылок перестала шуметь.** 16 «подозрительных appid» оказались корректными парами
с приписками изданий («Death Stranding» ↔ «DEATH STRANDING DIRECTOR'S CUT»). Сравнение
названий переписано: убираются марки, притяжательные «'s», приписки изданий (Director's Cut,
Ultimate Edition, Final Cut, Anniversary…), склеиваются сокращения с точками (S.T.A.L.K.E.R.),
учитывается вложенность названий и разница только в номере части. Из 16 осталась одна пара,
и она объяснена в явном списке `STEAM_ALLOW`: на ПК Uncharted 4 выходила только в сборнике
Legacy of Thieves Collection — это тот же appid, что и стоит в данных.

**Проверка обложек больше не «мигает».** CDN магазинов отвечает 403/429/503 на адреса CI;
раньше это считалось битой ссылкой, и один и тот же коммит падал в PR-прогоне и проходил
в push-прогоне. Теперь такие ответы и сетевые сбои идут в «не удалось проверить» (до трёх
попыток), ошибкой остаются только 404/410; без сети проверка останавливается после 20
запросов, а итог честно сообщает, сколько URL реально ответило.

## Что осталось на следующий заход (честно)

1. **Одна игра без ссылки на магазин — Football Manager 2024.** Она снята с продажи в Steam и
   Epic 05.11.2025, официальная страница `footballmanager.com/games/football-manager-2024`
   отдаёт роботам 403 (проверено в CI), заголовок раздела `/games` — «Football Manager 27».
   Пока подтверждённой страницы нет, кнопки у игры нет. Добавлять «ФМ 2024 In-game Editor»
   или поисковую ссылку нельзя — это другой продукт/заглушка.
2. **Игр без страницы в Steam больше нет в списке «надо найти».** CI искал страницы для всех
   игр без appid и находил чужие продукты: `Minecraft` → Minecraft Dungeons II,
   `League of Legends` → CONVERGENCE: A League of Legends Story, `Football Manager 2024` →
   FM 2024 In-game Editor, `Arknights` → Arknights: Endfield. Правило «в названии на Steam
   не должно быть лишних слов» (`extraWords`) такие пары отклоняет — теперь «Предложений: 0».
   Этим играм даны официальные магазины (Xbox, App Store, HoYoVerse, Supercell, Konami и др.).
3. **PR #5** — мержит пользователь; заголовок и описание дополняются только через
   `gh api -X PATCH repos/nigerader-dev/Gustoplay/pulls/5`.
4. **Что нельзя проверить в песочнице (и не выдумано):** живое устройство (настоящий телефон),
   Lighthouse и внешние CDN обложек недоступны — вёрстка проверена в Chromium на 360–1920px,
   ссылки — в CI, обложки — по данным каталога.

## Чем это проверяется

* `npm run check:text` (входит в `test:all`) — орфография и типографика RU/EN: словари офлайн,
  единый стиль английского.
* `npm run check:stores` (CI, `links.yml`) — каждая ссылка ведёт на конкретную страницу
  игры, страница живая и называется как игра; для игр без Steam — официальный магазин.
* `npm run test` (smoke) — ссылка «Открыть в Steam» ведёт на `/app/<id>/`, поисковых
  заглушек нет; `npm run test:functional` — поведение отметок, фокус, доступность подписи.

## Ограничения среды (честно)

* В песочнице доступны только GitHub и npm: Steam, Nintendo eShop, PlayStation Store,
  Epic, Google Play, сайты издателей закрыты (проверено, ответ 000). Поэтому ссылки
  в п. 4–5 проверяются в CI (`links.yml`), где сеть есть, и только после проверки
  попадают в данные. Выдуманных адресов в каталоге нет: либо подтверждённая страница,
  либо официальный сайт издателя, либо кнопки нет.
* Игр, у которых нет страницы в Steam (Nintendo-эксклюзивы, мобильные, Battle.net,
  Epic-эксклюзивы), нельзя снабдить ссылкой на Steam — для них даётся официальный
  магазин/сайт издателя.
* Живые устройства (настоящий телефон) и Lighthouse в песочнице недоступны: вёрстка
  проверена в Chromium на 360–1920px, тач-цели — по метрикам, обложки — по данным каталога.
