# QA-отчёт — 25–26 сентября 2026 (выполнение QA-PLAN)

Прогонов было семь; последние два — по сообщениям пользователя (ссылки и «что-то пропадает / отключается»).
Итоговые цифры сьютов — ниже в сводке, детали по раундам — в разделах «Второй проход» … «Седьмой проход».

## Сводка

| Направление | Проверено | Найдено багов | Исправлено |
|---|---|---|---|
| Функционал (QA-PLAN §1) | 86 сценариев ✅ | 19 | 19 |
| Визуал (QA-PLAN §2) | 16 a11y + ревизия + 38 CSS-аудитов ✅ | 5 | 5 |
| Адаптив (QA-PLAN §3) | структурно все ширины ✅ (см. матрицу) | 8 зон | 8 |
| Кросс-браузер/перф (QA-PLAN §4) | фолбэки + чистые прогоны ✅ | 0 | — |

Автотесты после всех фиксов:

| Сьют | Результат |
|---|---|
| `npm run check` | Проблем: 0 (436 игр) |
| `npm run audit` | ✅ все записи корректны |
| `npm test` (smoke) | ✅ все проверки пройдены |
| `npm run test:functional` | **128/128** (+42 регресса: битый профиль, отсутствие эмодзи, слияние профилей, блок «продолжить») |
| `npm run test:a11y` | **19/19** (перебор уровней заголовков, 14 пар контраста, парность тегов) |
| `npm run test:css` | **59/59** |
| `npm run test:visual` | **85/85** — реальный Chromium, 5 ширин × 17 маршрутов, ❌ 0 |
| `npm run test:api` | 55/55 |
| `npm run test:e2e` | **34/34** (+14 регрессов: слияние профилей, немедленная отправка) |
| `npm run build` | 624 страницы |
| `npm run verify:build` | 29/29 |

## Найденные баги

| # | Где | Серьёзность | Что было | Как исправлено | Тест-регресс |
|---|---|---|---|---|---|
| 1 | `/results` | 🟡 | Переключатель «показывать сыгранное» мёртвый: обработчика `toggle-played` не существовало | Добавлен case в `js/app.js`: сохраняет `lastPreset` и пересчитывает | functional §5 |
| 2 | `/quiz` | 🟢 | Возврат в квиз начинал с 1-го вопроса при сохранённых ответах | Продолжение с первого неотвеченного | functional §4 |
| 3 | `/quiz` | 🟡 | Клик по опции скроллил страницу наверх (мучительно в списке из 29 жанров) | Скролл только при смене шага | ревью (скролл не ловится в jsdom) |
| 4 | `/catalog` | 🟡 | Поиск терял фокус после каждого пересчёта — печатать дольше 1 паузы нельзя | Возврат фокуса с кареткой в конец | functional §6 |
| 5 | `/catalog` | 🟢 | Тег «Уютное» дублировался в фильтрах | Убран дубль в `POPULAR_TAGS` | ревью |
| 6 | `/party` | 🟡 | `players=8` в адресе: подпись врала «8», пресет не подсвечивался | Кламп к 5+ в `party.js` | functional §7 |
| 7 | `/profile` | 🟡 | Ответы modes показывались сырыми id (`coop`, `solo`) | Подписи через `ANSWER_LABELS.modes` | functional §8 |
| 8 | `/account` | 🟡 | Кнопки «Завершить» у сессий не работали (список подгружается async, обработчики умирали) | Делегирование на контейнер | functional §10 |
| 9 | EN-локаль | 🟡 | Бейдж уверенности показывал сырые ключи (нет EN-переводов) | Добавлены `results.confidence(+hint)` | functional §11 |
| 10 | движок | 🟡 | 6 мёртвых сигналов в `PREFERENCES` (id из чужих словарей: `laugh`, `party`, `coopLocal`×2, `roguelike`, `sandbox`, `esports`) | Заменены на валидные теги | functional §11–12 |
| 11 | движок | 🟢 | `neighbours: bestSimilar > 0.12` — сравнение строки с числом, всегда null | `bestSim > 0.12` | functional §12 |
| 12 | движок | 🟡 | При пустом пуле фильтры игнорировались молча (`relaxed=false`) | `relaxed=true` при любом фолбэке | functional §12 |
| 13 | движок | 🟡 | Ответы квиза coop/pvp не усиливали кооп/PvP-игры (вес на несуществующих id) | Раскрытие через `MODE_GROUPS` | functional §12 |
| 14 | `quiz.js` | 🟢 | Дубль ключа `key: 'q.genres'` | Удалён | ревью |
| 15 | `/account` | 🟢 | Хардкод русских строк (`Проверьте e-mail`, `Аккаунт удалён`, `Браузер`) в EN-режиме | Ключи `emailBad/deleted/browser` в ru/en | ревью |
| 16 | `/account` | 🟢 | После выхода оставались чужие сессии и старые уведомления | Чистка `state` при выходе/смене вкладки; удалён мёртвый код | ревью |
| 17 | Google-вход | 🟢 | Кнопка Google всегда на русском (`locale: 'ru'`) | `locale: getLang()` | ревью (нужен Google JS) |
| 18 | `/catalog` | 🟢 | Селект сортировки без подписи для скринридеров | `aria-label` | a11y §2 |
| 19 | 404, `/game/*`, `/results`, `/profile` | 🟡 | Нет `h1` в пустых/ошибочных состояниях (a11y + SEO) | Добавлен `h1` в 4 местах | a11y §1 (18 маршрутов) |
| 20 | темы | 🟡 | 6 текстовых пар с контрастом < 4.5:1 (светлый muted, success; тёмные warn/danger/success) | Новые значения `#6b7280/#0e7a55/#e8a33d/#f27e77/#4cc38a` | a11y §4 (24 пары) |
| 21 | hero | 🟡 | `.btn-lg{width:100%}` из секции аккаунта протекал глобально — CTA hero всегда в столбик | Область видимости сужена до `.auth-card` | ревью |
| 22 | пресеты | 🟢 | Крестик на пресетном чипе обещал снятие, но ничего не делал | Пресетные чипы без крестика | functional §13 |
| 23 | шторка (новое) | 🟡 | Esc закрывал шторку, но оставлял блокировку скролла | Снятие `body.filters-open` | functional §13 |
| 24 | i18n | 🟢 | README/QA-PLAN врали «11 вопросов» (их 15) | Доки обновлены | — |

Не баги (проверено, оставлено как есть): `completedAt` ставится при первом ответе (семантика «квиз начат»,
интерфейс консистентен); лимит «показать ещё» ~120 карточек (достаточно); `--star` недоступного
оттенка, но переменная нигде не используется.

## Что сделано по адаптиву (QA-PLAN §3)

- **Бургер-меню** (≤900px): выдвижная панель + оверлей + Esc + возврат фокуса + `aria-expanded/controls`.
- **Шторка фильтров** (≤900px): нижняя панель с бейджем числа активных, кнопкой «Показать N»,
  переживает выбор фильтров и перерендеры.
- **Мобильный hero**: вместо `display:none` — компактная полоса арта (150px → 120px).
- **Брейкпоинты**: 1080 / 900 / 640 / **480** (новый, компактные телефоны).
- **Тач-цели ≥44px**: кнопки, отметки, чипы, табы, опции, свитчи, инпуты, иконки шапки.
- **Компактные карточки** на телефоне: максимум 2 чипа, описание в 3 строки, подписи отметок только у активной.
- **Sticky-навигация квиза** на телефоне + `safe-area-inset`.
- **Защита от переполнения**: `min(Xpx,100%)` во всех сетках, `overflow-x: clip`, `overflow-wrap`,
  ellipsis для длинных названий/меток.
- **Фолбэки**: plain-дубли всех `color-mix`, `@supports` для `backdrop-filter`.

## Адаптив: матрица проверок

Легенда: ✅ — структурно и поведенчески проверено (`test:css` 38 + `test:functional` §13 + `test:a11y`).
Пиксельный контроль — `npm run test:visual` на машине с Chrome (в песочнице браузера нет, скрипт готов).

| Страница | 360 | 480 | 768 | 1024 | 1440 | 1920 |
|---|---|---|---|---|---|---|
| `/` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| `/quiz` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| `/results` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| `/catalog` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| `/genre/*`, `/tag/*`, `/mode/*`, `/mood/*`, `/platform/*` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| `/game/balatro` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| `/party` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| `/profile` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| `/account` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| `/terms`, `/about`, `/privacy`, 404 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

Проверено также: альбомный телефон (зоны 640–900 без «мёртвых» сеток), зум 200% (эквивалент 720px —
планшетная вёрстка), `prefers-reduced-motion`, тёмная тема на всех ширинах (контрасты кодом).

## Второй проход: визуальный QA в реальном браузере (25 сентября 2026)

Первый проход был статическим (CSS-аудиты + jsdom). Второй — живой Chromium: 17 маршрутов ×
5 ширин (360…1920), замеры переполнения, ошибок консоли, h1, битых картинок, тач-целей.

### Найденные баги

| # | Где | Серьёзность | Что было | Как исправлено | Тест-регресс |
|---|---|---|---|---|---|
| 25 | все страницы @360–768 | 🔴 | Страница ехала вбок на ~330px: у скрытой шторки меню (`transform: translateX(105%)`) габарит попадал в прокручиваемую область документа. На телефоне получался горизонтальный скролл и «болтающийся» контент | `body:not(.menu-open) .header { overflow-x: clip }` — обрезаем вылет ровно там, где шторка ждёт открытия (её содержащий блок — `.header`, его создаёт `backdrop-filter`); `overflow-x: clip` на `html` как страховка | test:css §5б + shots |
| 26 | меню на планшете/телефоне | 🔴 | Панель меню была высотой ~68px вместо экрана: `position: fixed` + `bottom: 0` считались от `.header`, а не от вьюпорта (снова `backdrop-filter`). Пункты наезжали друг на друга и обрезались | Явная высота `height: 100vh; height: 100dvh` вместо `bottom: 0` | test:css §5б + скриншот меню |
| 27 | меню + баннер согласия | 🟡 | Баннер согласия (z-index 60) перекрывал шторку меню: она живёт внутри `.header` с z-index 40 | `body.menu-open .header { z-index: 95 }` | test:css §5б |
| 28 | баннер согласия | 🟡 | Анимированный `transform` затирал `translateX(-50%)` — баннер стоял не по центру и вылезал за правый край (до 200px вылета) | Центрирование полями: `left: 0; right: 0; margin-inline: auto` без transform | test:css §5б |
| 29 | `/results`, `/profile`, `/quiz` | 🟡 | Профиль из localStorage со скаляром вместо массива (`modes: 'coop'` — так писался профиль в старой версии и при импорте) ронял страницу с `TypeError`: белый экран | `answerList()`/`normalizeAnswers()` в `js/quiz.js`; нормализация на входе в `store.js` (чтение, импорт, ответы квиза) и в движке (`computeWeights`, `hardFilter`, `scoreGame`, `summarize`) | functional §14 (14 проверок) |
| 30 | `/profile` @768 | 🟢 | Кнопка «Убрать» в списке отметок 36px — меньше тач-цели 44px (планшет — тоже касание) | `.btn-sm { min-height: 44px }` в блоке ≤900px | shots (мелкие тач-цели) |
| 31 | обложки/консоль | 🟢 | Ошибки недоступных внешних CDN (Steam, Google Play) попадали в отчёт наравне с дефектами сайта и при прогоне без сети выдавали «0/436 обложек битые» | `tools/shots.mjs`: свои ошибки и внешние считаются раздельно (`--strict` включает внешние); `tools/check-covers.mjs`: отдельная диагностика «нет доступа к CDN» вместо «все обложки битые» | прогон + `check:covers` |

Кроме этого, в `tools/shots.mjs` убраны ложные срабатывания самого инструмента: снимок брался
после таймаута `networkidle0` (с недоступным CDN сеть не затихает никогда — отсюда `h1=0`
на части страниц), сид профиля писался скаляром `modes: 'coop'`, порт сервера был жёстко
зашит (5199), а исключение из инжекта localStorage тоже попадало в отчёт как «ошибка страницы».
Провалом теперь считается то, что реально видно пользователю: страница прокручивается по
горизонтали, нет ровно одного `h1`, «undefined» в тексте, ошибки консоли/битые картинки
своего origin, страница не отрисовалась.

### Итог второго прохода

```
check (436 игр) ✅ 0 проблем · audit ✅ · smoke ✅ · verify:build 27/27
test:functional 120/120 · test:css 44/44 · test:a11y 16/16
test:api 55/55 · test:e2e 20/20 · test:reload 8/8
test:visual 85 проверок (17 маршрутов × 5 ширин) · ❌ 0
check:covers: 0 заглушек среди 436 обложек (сетевая часть — в CI)
```

Третий проход — сверка с планом сессии (см. `docs/HANDOVER.md` → «Сверка с планом»). Добавлено:
проверка, что обложки не заглушки, а официальные арты (`check:covers`, работает офлайн);
тест на отсутствие эмодзи в интерфейсе (§15); команда `npm run deploy:purge`; в деплое
сверяются версии сборки на всех публичных хостах, а не только на apex.

Не-дефекты, зафиксированные осознанно: чекбокс «показывать сыгранное» 22×22 — цель нажатия
даёт `label.switch` (44px); мелкие инлайн-ссылки в тексте (18–29px) — исключение WCAG 2.2 для
ссылок внутри текста; `smallCount` в отчёте информационный и прогон не красит.

## Четвёртый проход: отметки, шторка, ссылки, тексты (26 сентября 2026)

Третий проход был про инструменты и данные. Четвёртый — про то, что видит и трогает
пользователь: поведение кнопок под курсором, меню на телефоне, магазины, орфографию.

### Найденные баги

| # | Где | Серьёзность | Что было | Как исправлено | Тест-регресс |
|---|---|---|---|---|---|
| 32 | карточки игр, кнопки отметок | 🔴 | «Понравилось» и «Не понравилось» под курсором сходили с ума: подпись включалась в поток по наведению (`display: none → inline`), кнопка росла с 44 до 167px, соседние переносились на другую строку (`left 537→343`), курсор терял кнопку → цикл `mouseover/mouseout`. На «Играл» подпись оставалась → баг не проявлялся | Подпись всегда вне потока (текст остаётся для скринридера), при наведении меняется только цвет, подсказка — нативным `title`; убран и `transform: translateY(-1px)` по наведению (у края кнопки он давал тот же цикл) | `test:visual` (интерактив с включённым hover), `test:css` §6б |
| 33 | шапка, 901–1920px | 🔴 | Шесть пунктов меню не влезали в 1200px: «Как это работает» уезжало на вторую строку, шапка вырастала с 68 до 111px, справа оставалось пустое место; подпись логотипа рвалась на две строки | Рабочая ширина 1280px, подпись логотипа не переносится и скрывается ниже 1500px, пункты меню плотнее и без переноса, шторка включается с 1140px | `test:css` §5б, замеры в браузере на 12 ширинах |
| 34 | кнопка «Открыть в Steam» | 🔴 | Ссылка вела на **поиск по названию**: appid читался из сырой записи, а он приходит из `steam-covers.js` и подмешивается позже — условие не выполнялось никогда | appid читается после подмешивания: 402 игры ведут на `/app/<id>/` (проверено: Balatro → 2379780, The Witcher 3 → 292030) | `npm test` (smoke) проверяет вид ссылки |
| 35 | сторонний магазин | 🟡 | В блоке покупки была кнопка Instant Gaming (поисковая ссылка), добавленная без запроса | Магазин убран целиком: данные, кнопка, ключи i18n, конфиг, документация | `grep` по сайту, `check:stores` |
| 36 | описания игр | 🟡 | Опечатки и выдуманные слова: «ложня» (Lies of P), «превщают» (The Binding of Isaac), «Рогалевания» (Dead Cells), «Рогалайк» (PlateUp!, Lethal Company), «фантастка и фэнтезистка» (It Takes Two) | Исправлено в источнике контента (`tools/content/*.mjs`) → перегенерирован каталог; стиль английского приведён к британскому (`tower defence`, `civilisation`) | `npm run check:text` (5160 текстов) |
| 37 | текст интерфейса | 🟢 | «{wishlist} в вишлисте» рядом с кнопкой «Хочу сыграть»; в FAQ — «Пересчёт мгновенный и обратимы»; FAQ обещал «обычные ссылки на поиск», которых больше нет | Формулировки приведены к словарю интерфейса и к фактическому поведению | `check:text`, ручная вычитка всех 306 строк RU |
| 38 | карточки @360 | 🟢 | Длинное слово в описании обрезалось краем карточки (145px в 133px) | Перенос длинных слов в заголовках, описаниях и буллетах | `test:visual` (проверка обрезанного текста) |
| 39 | форма «На компанию» @1920 | 🟢 | Панель «Только бесплатные» с одним переключателем растягивалась до высоты самой высокой панели | `align-items: start` — панели по своему содержимому | скриншот `party-1920` |
| 40 | поле поиска в фильтрах | 🟢 | Плейсхолдер обрезался на середине слова («Поиск: название, жанр, т…») | Короткий плейсхолдер: «Поиск по названию или жанру» | скриншот `catalog-1440` |

Отдельно: у шторки меню ширина уменьшена с 88vw до 80vw — на 360px за панелью
оставалось 43px страницы (12%), теперь 72px (20%), а кнопка закрытия 44×44
и возврат фокуса на бургер проверяются автоматически.

### Итог четвёртого прохода

```
test:visual  102 проверки (17 маршрутов × 6 ширин: 360/768/1024/1141/1440/1920) · ❌ 0
             + интерактивные проверки: наведение на отметки, шторка, CLS, обрезка текста
test:css     52/52   test:functional 120/120   check:text 5160 текстов · 0 ошибок
smoke        ✅      check (436 игр) ✅          audit ✅
```

Проверки, которые появились именно в этом проходе (чтобы баги не вернулись):
наведение на отметки в реальном браузере **с включённой мышью и hover** (в headless
Chrome по умолчанию `hover: none` — из-за этого баг №32 раньше не воспроизводился),
шторка (доля экрана, крестик, Esc, затемнение, фокус, отсутствие горизонтальной
прокрутки), сдвиги макета (CLS 0.000 на загрузке, при открытии меню и наведении),
обрезанный по ширине текст, орфография и типографика RU/EN.

## Шестой проход: ссылки доведены, мелочи вёрстки закрыты (26 сентября 2026)

### Баги, найденные в браузере на 360px

| # | Где | Серьёзность | Что было | Как исправлено | Тест-регресс |
|---|---|---|---|---|---|
| 41 | «Мой вкус», список отметок | 🔴 | Строка отметки была 364px в панели 294px: колонка грида растягивалась под неразрывное название игры («Deep Rock Galactic»), а `flex-строка` не сжималась — подпись статуса уезжала за край экрана и обрезалась. Страница вбок не ехала: вылет скрывал `overflow-x: clip`, поэтому проверки его не видели | Колонка `minmax(0, 1fr)`, у строки `min-width: 0`; на ≤640px строка — сетка «обложка \| название и статус \| крестик», название переносится, а не обрезается многоточием | `test:visual` §E (вылет за экран на 5 маршрутах × 360/768), `test:css` §5б |
| 42 | «Мой вкус», шкалы интересов | 🟡 | Колонка подписи была жёстко 150px: 8 тегов из 10 («Сплитскрин/локальный кооп», «Не требует мощного ПК», «Высокая реиграбельность»…) обрезались многоточием на всех ширинах, включая 1024px | Колонка по содержимому (`max-content`), на телефоне подпись встаёт над шкалой на всю ширину; замер: обрезанных подписей 0 из 10 на 360/480/640/768/1024 | `test:css` §5б |
| 43 | квиз, липкая панель «Дальше» | 🟢 | Фон панели был полупрозрачным (90%) без размытия: под липкой панелью проезжала карточка вопроса и текст читался сквозь кнопки | Фон с размытием, как у шапки, и сплошной фолбэк через `@supports not (backdrop-filter)` | `test:visual` §Ж (панель липнет к низу, кнопка не обрезана, фон не просвечивает), `test:css` |

| 44 | шапка и панель квиза на 280–320px | 🟡 | На 280px (сгибаемый телефон) шапка была 310px: кнопка меню уезжала за край, меню нельзя было открыть. На 320px три кнопки панели квиза не влезали: «Дальше» заканчивалась на 327px при экране 320px | На ≤320px скрывается название бренда (иконка остаётся, шапка 194px), на ≤360px кнопки квиза компактнее, перенос кнопок — страховка | `test:visual` §E (5 маршрутов × 280/320/360/768, вылет за экран и обрезка текста), `test:css` |

| 45 | карточки игр на 360–480px | 🟡 | Слова и числа рвались посередине: студия «ConcernedApe» → «ConcernedA» + «pe», цена «2500 ₽ и выше» и длительность «50–200» ломались внутри числа. Замер: на первом экране каталога при 360px — 16 разорванных слов (в 47 описаниях из 436 есть слово шире строки: 133px при карточке 159px) | Цена разделена на два span'а (переносятся как целые группы), «год · студия» — flex с переносом целых span'ов, подвал карточки не рвёт группы (`white-space: nowrap`), длинные слова в описаниях — `hyphens: auto` (по слогам; на телефонах словарь есть) + `break-word` как страховка | `test:visual` §З — обход карточек на 360/480: числа, цены и подписи не рвутся; для описаний отдельная строка (в Chromium без RU-словаря переносов такие слова всё равно рвутся — ограничение браузера, не вёрстки) |

Отдельно: **тёмная тема** впервые снята скриншотами целиком (`--theme=dark --shots=all`):
85 проверок, ❌ 0, контрасты проверены в `test:a11y` (12 пар на тему). Заодно исправлен
инструмент: скриншоты светлой и тёмной темы писались в один файл и затирали друг друга —
теперь тема в имени файла.

| 46 | порядок заголовков | 🟡 | Уровни перескакивали почти везде: h1 → h3 на каталоге (карточки), h2 → h4 в подвале, h1 → h4 в «Условиях», h1 → h3 в квизе/профиле/игре. Нашёл Lighthouse (heading-order), свои проверки смотрели только «ровно один h1» | Уровень выбран по структуре, размер — по роли (`.h-lg`/`.h-base`); 14 маршрутов идут последовательно, регресс в `test:a11y` | `test:a11y` (перебор уровней), Lighthouse heading-order = 1.0 |
| 47 | контраст и доступное имя | 🟡 | `--text-muted` на `--bg-sunken` давал 4.24 (порог 4.5) — задевало подпись и текст блока рекламы; у ссылки-обложки видимые «96» и «1–4» не входили в `aria-label` (WCAG 2.5.3) | `--text-muted` стал `#676d7f` (4.53), пары добавлены в проверку контрастов; обложка, оценка и плашка — соседи в обёртке, плашки `pointer-events: none` | `test:a11y` (14 пар на тему), Lighthouse color-contrast и label-content-name-mismatch = 1.0 |
| 48 | несжатая сборка | 🟠 | CSS и JS уезжали на хостинг как есть: 21 КиБ лишнего в CSS и 70 КиБ в JS (Lighthouse unminified-*). На localhost это не видно — файлы маленькие | `npm run build` минифицирует CSS (со сверкой числа объявлений) и JS (esbuild, `charset: utf8`); `verify:build` это проверяет | Lighthouse: performance 67 → 94, FCP 4.1 → 2.1 с, LCP 6.7 → 2.8 с |

**Lighthouse (впервые снят локально, на собранном сайте).** Итог: performance **94**,
accessibility **100**, best-practices **96**, SEO **100**, CLS **0**. Показатель
best-practices не 100 из-за ошибок сети: обложки лежат на CDN Steam, который из песочницы
недоступен (в CI и у пользователя они грузятся). Остальные замечания производительности —
тоже следствие закрытой сети (FCP/LCP ждут таймаутов картинок).

### Ссылки на магазины

* Перенесены ещё 5 подтверждённых CI страниц: Fortnite (Xbox), PUBG Mobile,
  Pokémon TCG Pocket, Whiteout Survival (App Store), Castlevania: Symphony of the Night (Konami).
* Итог: **ссылка есть у 435 игр из 436** — 401 страница Steam и 34 официальных магазина.
  Без ссылки — Football Manager 2024 (снята с продажи в Steam и Epic 05.11.2025,
  официальный сайт отдаёт роботам 403; проверено в CI).
* Две ошибки нашлись в самих инструментах проверки: аннотация GitHub режется на 4096 символах
  (список ссылок приходил обрезанным — теперь части `PART_i_OF_n` склеиваются в
  `tools/pull-store-links.mjs`), а `--apply` перезаписывал файл данных целиком и стирал ранее
  подтверждённые адреса (теперь данные сливаются — так вернулся PlayStation-адрес Bloodborne).

### Итог шестого прохода

```
test:all     ✅ целиком: check (436 записей, проблем 0), check:text (5175 текстов, 0 ошибок),
             audit (436 корректны), smoke ✅, functional 120/120, a11y 16/16, css 56/56,
             visual 85 «маршрут × ширина» + 95 интерактивных (❌ 0), светлая и тёмная тема, api 55/55,
             e2e 20/20, reload 8/8
CI           «Тесты и сборка» success, «Ссылки на магазины» success:
             мёртвых appid 0, подозрительных 0, предложений 0, не проверено 0, без ссылки 1
```

## Седьмой проход: «что-то пропадает или отключается» (26 сентября 2026)

Пользователь попросил починить всё, из-за чего данные или функции исчезают сами. Прошёл по
цепочке «отметка → localStorage → аккаунт → сервер → возврат на устройство» и по всем местам,
которые что-то скрывают, гасят или обнуляют. Нашлось десять причин — от потери отметок до
исчезающей плашки; все закрыты, на каждую есть проверка.

| # | Где | Серьёзность | Что было | Как исправлено | Тест-регресс |
|---|---|---|---|---|---|
| 49 | синхронизация профиля (`app.js`, `account.js`, `store.js`) | 🔴 | Профиль с сервера **подменял** локальный целиком (`replaceProfile`). Если человек поставил отметку и перезагрузил страницу раньше, чем автоотправка (4 с) успевала уйти, отметка исчезала. Вместе с ней сбрасывались тема и язык — они тоже лежат в профиле | Появилось слияние (`mergeProfiles` → `adoptRemote`): отметки — по более свежей дате, показы — по максимуму, история — объединение, ответы — по более позднему прохождению, а настройки устройства (тема, язык) всегда остаются локальными. Объединённый профиль уходит обратно на сервер | `test:e2e` §4 (13 проверок, включая «после перезагрузки локальные отметки на месте», «тема не сброшена») |
| 50 | автоотправка профиля | 🟠 | Отправка стояла только на таймере 4 с: закрытие вкладки, переход по внешней ссылке и офлайн-момент теряли последние правки | Отправка по `pagehide`, `visibilitychange` и `online` через `fetch(keepalive: true)`; выход из аккаунта сначала дошлёт профиль, потом чистит токен | `test:e2e` §4: «уход со страницы отправляет профиль сразу, без ожидания таймера» |
| 51 | импорт копии профиля (`views/profile.js`) | 🔴 | «Загрузить копию» **заменяла** профиль: экспорт с телефона, импорт на ноутбуке — и отметки ноутбука исчезали молча | Импорт объединяет данные по тем же правилам, что синхронизация, и сообщает результат («Данные объединены с текущими · отметок: N»); явная замена осталась только программно (`{ replace: true }`) | `test:functional` §8 (2 проверки), `test:e2e` §4 |
| 52 | вход по сохранённому токену (`app.js`) | 🟠 | Любая ошибка проверки сессии — включая пропавшую на минуту сеть — вызывала `clearSession()`: аккаунт «отключался» сам, приходилось входить заново | Сессия сбрасывается только при явном 401; при сетевой ошибке профиль и токен остаются | `test:e2e` §3 (выход/вход), ручная проверка сценария offline |
| 53 | слияние ответов квиза | 🟠 | Свежий, но неполный профиль затирал заполненные поля: `genres` из второй копии исчезали из «Моего вкуса» (поймал собственный тест на слияние) | Пустое значение больше не перекрывает заполненное (`mergeAnswers`) | `test:e2e` §4: «ответы квиза объединяются, свежие главнее» |
| 54 | предел профиля и отказы сервера | 🟠 | Лимит отметок `CONTENT.maxMarks` в конфиге не работал, а сервер (256 КБ) на слишком большой профиль отвечает 413 — автоотправка падала молча, интерфейс продолжал считать, что всё сохранено | Лимит применяется, при отказе сервера причина показывается в интерфейсе (полоса предупреждения), при успешной отправке — исчезает | `test:functional` (лимит отметок), ручная проверка ветки ошибки |
| 55 | «Активные сессии» (кабинет) | 🟡 | Пустой список и ошибка запроса выглядели как вечная «Загрузка…», а флаг `AUTH.sessionsList` ни на что не влиял (в разметке стояло `${AUTH.sessionsList ? '' : ''}`) | Флаг реально скрывает панель, у пустого списка есть текст, у ошибки — тоже (загрузка больше не залипает) | `test:e2e` §3 (кабинет), `test:functional` |
| 56 | плашка «Показать обновлённый подбор» | 🟡 | Она исчезала через 12 секунд, а список не пересчитывался: не заметил — подбор выглядел так, будто отметку проигнорировали. Главная при этом обещала «список пересчитается на ходу» | На странице подбора отметка пересчитывает список сразу; вместо кнопки-плашки — короткое подтверждение «Подбор обновлён» (не перехватывает клики) | `test:functional` §5 (4 проверки: пересчёт, сообщение, самоубирание) |
| 57 | возврат к результатам с главной | 🟡 | Блок «продолжить» показывался только при отметках: человек, прошедший квиз без отметок, не видел ни результатов, ни продолжения — выглядело, будто ответы пропали | Блок учитывает прогресс квиза: «Вы ответили на N% — можно продолжить» и «Подбор готов» со ссылкой на результаты | `test:functional` §4б (3 проверки) |
| 58 | service worker (`sw.js`, `app.js`) | 🟡 | При выкладке в подпапку (GitHub Pages) регистрация шла по жёсткому `/sw.js` (404 — офлайн-режим молча выключался), предзагрузка указывала на чужой корень, а `/api/*` в подпапке не распознавался (ответы сервера попадали в кэш страниц). Плюс `cache.addAll` падал целиком из-за одного недоступного файла | База берётся из адреса воркера (`/sw.js`, `/sub/sw.js`, `/a/b/sw.js` — проверено), API распознаётся с базой, файлы кэшируются по одному | ручной прогон логики воркера (BASE/isApi/PRECACHE) для трёх вариантов баз, `verify:build` |
| 59 | разметка кабинета | 🟡 | Пять заголовков были открыты как `<h2>`, а закрыты как `</h3>` — браузер «чинил» это сам, уровень заголовка отличался от задуманного | Теги приведены в порядок, добавлена постоянная проверка парности заголовков по исходникам | `test:a11y` §5: «уровень заголовка совпадает с закрывающим тегом» |
| 60 | мёртвые флаги конфига | 🟡 | Семь настроек в `js/config.js` никто не читал: `FEATURES.realCovers`, `scoreDebug`, `exportProfile`, `localOnlyFallback`, `CONTENT.minPool`, `AUTH.syncDebounce`, `ADS.autoAds`. Владелец включал флаг — не менялось ничего | Подключены: обложки владельца реально в приоритете (с двумя ступенями отката на арт магазина и генерацию), `scoreDebug` управляет блоком «Почему подходит», `exportProfile` — кнопкой выгрузки, `syncDebounce` — задержкой автоотправки, `autoAds` — авторекламой AdSense. Удалён не нужный `localOnlyFallback` (локальный режим и так работает) и `minPool`; несделанные пока флаги (`requireEmailVerification`, `sensitiveRateLimit`) честно помечены в конфиге | `test:functional` (обложки, импорт/экспорт), `smoke` (фолбэк обложек) |
| 61 | недоступное хранилище браузера | 🟡 | Приватный режим или переполненное хранилище — запись в `localStorage` падала молча: отметки «пропадали» после перезагрузки без объяснения | Факт запоминается и показывается полосой «Браузер не сохраняет данные…»; чтение тоже обёрнуто (в приватном режиме обращение к хранилищу может бросать исключение) | `test:functional` §8 (битый импорт), code review |

Заодно: `results.js` и главная считали «профиль отвечен» по `meta.completedAt`, которое
обновляется при **каждом** ответе, — теперь по доле отвеченных вопросов (`progress`).
Тесты, где «главная» открывалась как `navigate('home')`, попадали на страницу «не найдено»
(роут главной — пустой путь) и проходили вхолостую — исправлено на `navigate('')`.

## Скриншоты

Пиксельный прогон выполнен: **85 проверок (5 ширин × 17 маршрутов), провалов 0**,
кадры лежат в `docs/qa-shots/` (в git не коммитятся, см. `.gitignore`).

```bash
npm i -D puppeteer && npx puppeteer install chrome     # обычная машина
npm run test:visual -- --widths=360,768,1024,1440,1920 --shots=all
```

Там, где Chrome скачать нельзя (закрыт `storage.googleapis.com` — песочницы, корпоративные
прокси), браузер берётся из npm-пакета и запускается одной командой:

```bash
node tools/chromium-offline.mjs --run -- --widths=360,768,1440 --shots=all
```

`tools/chromium-offline.mjs` ставит `@sparticuz/chromium` (бинарь внутри npm-тарбола),
распаковывает к нему бандл системных библиотек и передаёт `LD_LIBRARY_PATH` +
`PUPPETEER_EXECUTABLE_PATH` в прогон. Из инструментального: `tools/shots.mjs` сам поднимает
сервер на свободном порту, ждёт отрисовку (`#app .header`) и делит ошибки на «свои» и
внешние (недоступный CDN обложек — не дефект сайта, печатается отдельной строкой; `--strict`
включает и их).

## Что не сделано / риски

1. **Пиксельный прогон** (`test:visual`) — выполнен (см. «Второй проход» и «Шестой проход»).
2. **Живые устройства** (iOS Safari, Android Chrome, Firefox): в песочнице недоступны, вёрстка
   стандартная, но тач-скролл шторок и 100vh-особенности iOS стоит пощупать руками
   (vh в шторках не используется — риск низкий).
3. **Lighthouse-замеры** — сняты, см. «Шестой проход» (performance 94, accessibility 100,
   SEO 100, CLS 0 на собранном сайте); цифры зависят от машины, поэтому указаны как ориентир.
4. **Мигание темы** при первой загрузке (тёмная применяется JS после парсинга): inline-фикс
   невозможен — CSP запрещает инлайн-скрипты. Проявляется как ~50мс светлой темы. Не фиксится
   без смягчения CSP — осознанно не трогаем безопасность ради косметики.
5. **Печать** страниц — пропущена (в плане помечена опциональной 🟢).
