# Контент-план: вкладка «На компанию», скролл, отметки, блоки страницы игры, требования к ПК, почта поддержки

> **Ветка:** `arena/01a0ddd3-gustoplay` · **Составлен:** 26 сентября 2026 · **Статус:** 🔄 в работе
> (пункты 1–3, 5, 6 — сделаны и проверены; пункт 4 — сборщик исправлен, идёт повторный прогон;
>  пункт 7 — прогоны и сдача)
>
> Формат статуса: ⬜ не начато · 🔄 в работе · ✅ сделано · ⚠️ ограничение (объяснено).
> Правило сессии: никаких заглушек и выдуманных данных. Если данных нет — это честно написано
> в интерфейсе и в отчёте, а не заменено правдоподобным вымыслом.

## Что просил пользователь (дословно по смыслу)

1. На вкладке «На компанию» показывается всего 24 варианта — должны показываться **все** подходящие
   под фильтр игры этой вкладки, и должна быть кнопка «Показать ещё».
2. В каталоге кнопка «Показать ещё» **перекидывает в самый верх страницы** — так быть не должно:
   остаёмся там, где нажали.
3. В карточках отметок иконка «не понравилось» должна быть **большим пальцем вниз**, вся линейка
   отметок — в одной стилистике.
4. На странице игры: четыре вертикальных блока и один под ними. Нижний надо **расширить на всю
   ширину четырёх** и сделать горизонтальным, **убрать из него содержимое** и **перенести туда
   содержимое блока «Особенности»**; на освободившееся место (где были «Особенности») —
   **минимальные и рекомендуемые требования к ПК**. Все блоки должны быть примерно одинаково
   заполнены, без больших пустот.
5. Почта `gustoplaysupport@gmail.com` — **основная почта сайта** для фидбека, помощи и поддержки.

## Разведка (что уже выяснено в коде)

* `js/views/party.js` — `recommendForParty({ …, limit: 24 })`, кнопки «показать ещё» нет;
  в заголовке `tp('party.result', games.length)` печатает число из выдачи (24), а строка
  «11 игр с коопом за одним экраном» считается по тем же 24 играм.
* `js/engine.js` — `recommendForParty()` уже умеет возвращать весь пул: `limit` только режет готовый
  отсортированный список в конце.
* Каталог: кнопка `data-action="show-more"` → `catalog.updateQuery({ page })` → `navigate(..., { replace })`
  → `gf:navigate` → `render(true)` → `window.scrollTo({ top: 0 })`. Отсюда прыжок наверх.
* Отметки (`js/views/components.js` → `markButtons`): played — `controller`, liked — `heart`,
  disliked — `minusCircle`, wishlist — `bookmark`. Иконок `thumbsUp`/`thumbsDown` в `js/icons.js` нет.
* Страница игры (`js/views/game.js`): `.game-details` — сетка `repeat(auto-fit, minmax(240px, 1fr))`
  из **пяти** карточек: «Об игре» (факты), «Режимы · Платформы», «Жанры», «Теги» (+шкалы) —
  это четыре блока в строке, пятая карточка «Игроки» (только иконки платформ) уезжает под них
  и выглядит пустой. Блок «Особенности» — `.game-feats` внутри `.game-info` шапки игры
  (`t('game.features')`, буллеты `game.feats`).
* Требований к ПК в проекте нет ни в каком виде — только тег `lowsysreq`. В песочнице нет сети
  к Steam (ответ 000), поэтому данные Steam в проекте тянет **GitHub Actions**: так уже сделаны
  обложки (`tools/resolve-steam-covers.mjs` + `.github/workflows/covers.yml`, доставка данных через
  аннотации прогона — `tools/emit-cover-data.mjs` + `tools/collect-cover-data.mjs`).
  В `js/catalog/steam-covers.js` лежит `steamId` для 401 игры — это ключи для Store API.
* Почта живёт в `SITE.email` (`js/config.js`; раньше `hello@gustoplay.ru`, теперь
  `gustoplaysupport@gmail.com`) и выводится в подвале (`js/app.js`), в политике конфиденциальности
  и на её отдельной странице, в условиях (`js/views/terms.js`).

---

## План работ

| № | Пункт | Статус | Как проверяю |
|---|---|---|---|
| 1 | **Вкладка «На компанию»: весь пул + «Показать ещё».** `recommendForParty` возвращает полный ранжированный пул; вид режет его по `FEATURES.pageSize` и показывает кнопку «Показать ещё N» (`data-action="show-more"`, `party.showMore`/`reset` в `js/app.js`, как у «Результатов»). В заголовке — общее число подходящих игр, строка про кооп за одним экраном — тоже по всему пулу | ✅ | `test:functional` (число подходящих > 24 и совпадает с расчётом по данным), `test:visual` (кнопка есть, нажатие добавляет карточки, список доходит до конца пула) |
| 2 | **«Показать ещё» не бросает страницу вверх.** `navigate(path, { replace, keepScroll })`; `render(scroll)` не сбрасывает скролл для пагинации; каталог вызывает `updateQuery(patch, { keepScroll: true })`. Работает и в каталоге, и на новых кнопках «На компании»/«Результатах» | ✅ | `test:visual`: замер `scrollY` до/после нажатия (сдвиг ≤ 2px) в каталоге и на вкладке компании; регресс в `test:css` не нужен, но в `test:functional` — проверка, что обработчик передаёт флаг |
| 3 | **Иконки отметок в одной стилистике.** В `js/icons.js` добавляются контурные `thumbsUp`/`thumbsDown` (24×24, `currentColor`), в `markButtons` пары становятся: «играл» — геймпад, «понравилось» — палец вверх, «не понравилось» — палец вниз, «хочу сыграть» — закладка | ✅ | `test:functional` (состав иконок отметок), `test:visual` (SVG отрисованы, размеры кнопок не поехали) |
| 4 | **Требования к ПК: настоящие данные из Steam.** `tools/pull-system-requirements.mjs` тянет `pc_requirements` (ru+en) для 401 игры с `steamId` — **по одному appid за запрос** (Steam отвечает 400 на список), с ретраями, задержкой и повтором без `filters` там, где магазин отдаёт пустой `data`; HTML магазина парсится в структуру `{ os, cpu, ram, gpu, dx, disk, note }`; пишется `js/catalog/sysreq.js` и отчёт `tools/sysreq-review.txt`. Доставка — как у обложек: workflow `.github/workflows/sysreq.yml` + `tools/emit-sysreq-data.mjs` + `tools/collect-sysreq-data.mjs` (аннотации прогона, если токен не может коммитить) | 🔄 | Прогон workflow через `gh workflow run`, отчёт о покрытии (сколько игр с min/rec, сколько без), `npm run check` после подключения данных |
| 5 | **Переверстка страницы игры по скриншоту.** Пятая карточка (`.detail-card`) теряет содержимое (иконки платформ дублировали карточку «Режимы · Платформы») и становится широкой на всю строку — в неё переезжают «Особенности» (горизонтальный список в 2–3 колонки). В шапке игры на месте «Особенностей» — блок «Требования к ПК»: минимальные и рекомендуемые. Пустот в блоках нет: у всех есть содержимое, длинная карточка заполнена по ширине | ✅ | `test:css` (правила широкой карточки и требований), `test:visual` (страница игры на 360/768/1024/1440/1920: нет вылета за экран, блоки не пустые), скриншоты в `docs/qa-shots` |
| 6 | **Почта поддержки.** `SITE.email = 'gustoplaysupport@gmail.com'` и понятные подписи: кликабельный `mailto:` в подвале («Поддержка»), в FAQ и политике («Как это работает», `/privacy`), в условиях использования. Старый адрес убирается из кода и документации, где он был контактом сайта | ✅ | `grep` по репозиторию (старых адресов-контактов нет), `test:functional` (почта видна и является ссылкой `mailto:`), `check:text` (новые ключи ru/en) |
| 7 | **Полный прогон и сдача.** `npm run check`, `check:text`, `audit`, `test`, `test:functional`, `test:a11y`, `test:css`, `test:visual` (реальный Chromium через `tools/chromium-offline.mjs`), `test:api`, `test:e2e`, `test:reload`, `build` + `verify:build`. Обновление `docs/FIX-PLAN.md` (новый раздел), коммит, пуш ветки, PR | 🔄 | Зелёные прогоны + отчёт в конце сессии, скриншоты прикладываются в `docs/qa-shots` |

## Порядок исполнения

Пункты 1–3 (логика и иконки) → 4 (данные требований, самый долгий шаг: сеть только через Actions) →
5 (вёрстка, использует данные из 4) → 6 (почта) → 7 (прогоны, отчёт, PR).

Пока идёт прогон workflow за требованиями, параллельно делаются пункты 1–3 и 6 — они от данных не зависят.

---

## Что показал первый прогон за требованиями (26.09.2026, прогон 36244465168)

Прогон workflow собрал **0 игр из 401** и это не скрыто: в отчёте `tools/sysreq-review.txt`
стоят 401 строка `MISS`, а в аннотациях — `sysreq-meta: 0 записей`.

Разбор причин (по аннотациям прогона, без догадок):

| Что нашли | Доказательство | Что сделано |
|---|---|---|
| Steam отвечает **HTTP 400 на запрос нескольких appid** сразу | все тридцать аннотаций: `батч 1..5 (russian/english) не получен: HTTP 400`, при этом одиночные запросы в пробнике дают нормальный ответ | сборщик переписан на **один appid за запрос** (`--batch=20` убран, задержка 250 мс, ~800 запросов), в `--probe` добавлена проверка пакетного запроса |
| **Ошибка разбора в парсере**: деструктуризация `const [, m, key] = [match, key]` давала `key = undefined`, и все поля уезжали в свойство `"undefined"` | локальная проверка: `parseRequirements('OS: Windows 11')` → `{"undefined": "S: Windows 11"}` | исправлено на `const [m, key] = found`, добавлена защита от повторного вхождения поля (значения склеивались без разделителя) |
| Единицы «ГБ/МБ» не приводились к общему виду, из-за чего одинаковые требования ru/en писались парой `{ ru, en }` | `{"ram":{"ru":"8 ГБ ОЗУ","en":"8 GB RAM"}}` | `unifyUnits` переписан: убирает «ОЗУ», «available space», «и более», `\b` заменён на проверку кириллицы без ASCII-края |
| Проверять это локально было нечем | — | новый `npm run test:sysreq`: поднимает **заглушку Steam** (в том числе отвечает 400 на несколько appid и пустым `data` на `filters`), прогоняет настоящий сборщик и проверяет файл и отчёт (29 проверок). Добавлен в CI |

## Второй и третий прогоны

**Прогон 2 (36246503453, коммит `c9555ed`) — 66 игр из 401 с настоящими требованиями.** Данные в ветке:
`a-way-out`, `afk-journey`, `armored-core-vi-fires-of-rubicon`, `beamng-drive`, `grand-theft-auto-v`,
`phasmophobia`, `raft`, `the-forest`, `wuthering-waves` и другие — минимальные и рекомендуемые, с ОС,
процессором, памятью, видеокартой, DirectX и местом на диске.

Что в разборе было не так (и исправлено):

| Что нашли | Пример из файла прогона 2 | Исправление |
|---|---|---|
| Метки со звёздочкой не распознавались | у части игр не было поля «ОС», хотя магазин его публикует («OS *:») | метка нормализуется (`*`, точки, пробелы по краям) |
| Неизвестные метки приклеивались к предыдущему полю | `"disk": "60 GB Поддержка VR: 10GB VRAM GPU…"`, `"sound": "/ Поддержка VR: /"` | строки с чужой меткой пропускаются, значение предыдущего поля не засоряется |
| Значения-разделители попадали в данные | `"note": "/"` | значение из одних `/`, `-`, `•` не считается значением |
| Прошлые записи переживали пересборку | при слиянии старый мусор оставался там, где прогон не получил свежих данных | запись с внутренней меткой («Слово:») не используется как запасная |
| Причин «нет данных» не было видно | 335 строк `MISS` без объяснения | в отчёте и аннотациях: причины (нет `pc_requirements` / `success=false` / запрос не прошёл), способ получения и 12 строк `DIAG` с HTTP-статусом, типом `data` и объёмом ответа |

**Прогон 3 (36248638115)** идёт с этими исправлениями: три потока запросов (упирались в 45-минутный
лимит джоба), третья попытка с витриной `cc=us` для игр, которые магазин иначе отдаёт пустым `data`,
и диагностикой в отчёте. Покрытие по итогу прогона будет дописано сюда.
